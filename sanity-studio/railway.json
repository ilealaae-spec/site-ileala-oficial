{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -f package-lock.json ../package-lock.json && npm config set package-lock false && npm config set prefer-offline false && npm config set ci false && npm install --no-package-lock --legacy-peer-deps --no-audit --no-fund && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s dist -l $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

