[build]
builder = "nixpacks"
workingDir = "ileala-website"
buildCommand = "pnpm install && pnpm run build"

[deploy]
startCommand = "pnpm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
